import { ExternalLink, ShieldCheck, Database, Scale, Code2, CircleHelp } from 'lucide-react'; import { CONTRACT_ADDRESS, explorerAddress } from '../config/chains'
export default function Docs(){return <div className="page docs"><div className="page-head"><div><div className="eyebrow">IN-APP DOCUMENTATION</div><h1>How GrantSeal works</h1><p>The frontend mirrors the deployed contract's actual lifecycle and public methods.</p></div></div><Doc title="Overview" icon={<ShieldCheck/>}><p>GrantSeal is a public workflow for open-source grant milestones. The grantor locks the scope before work begins, the recipient binds a submission to an exact Git commit, and GenLayer consensus resolves contested milestone outcomes from canonical GitHub records.</p></Doc><Doc title="How It Works" icon={<Scale/>}><ol><li>Create a draft program with a recipient, repository, title and scope.</li><li>Define milestones and criteria, then lock the program.</li><li>The recipient accepts the immutable program.</li><li>The recipient submits one exact commit SHA for a milestone.</li><li>Any non-recipient may challenge during the fixed response window.</li><li>The recipient may respond.</li><li>After the window, canonical GitHub repository and exact commit records are independently fetched and adjudicated.</li><li>The recipient or challenger may appeal, triggering a fresh independent round.</li><li>The grantor closes the program after every milestone is terminally resolved.</li></ol></Doc><Doc title="Architecture" icon={<Database/>}><p>The deployed contract stores two real entities: <b>Program</b> and <b>Milestone</b>. Program scope is locked before acceptance. Milestones carry locked criteria, an exact commit SHA, optional challenge/response context, a consensus outcome and optional appeal state.</p><p>Evidence endpoints are derived from locked identifiers. The returned GitHub repository must bind to the stored <code>owner/repo</code>, and the returned commit must bind to the exact stored SHA before evidence can influence a verdict.</p></Doc><Doc title="Smart Contracts" icon={<Code2/>}><p>Network: GenLayer StudioNet · Chain ID 61999.</p><p>Contract: <a href={explorerAddress()} target="_blank" rel="noreferrer">{CONTRACT_ADDRESS} <ExternalLink size={14}/></a></p><div className="api-list">{['create_program(recipient, repo_id, title, summary)','define_milestone(program_id, title, criteria_json)','lock_program(program_id)','accept_program(program_id)','submit_milestone(milestone_id, commit_sha, submission_note)','challenge_milestone(milestone_id, challenge_reason)','respond_to_challenge(milestone_id, response_text)','resolve_milestone(milestone_id)','appeal_milestone(milestone_id, appeal_reason)','resolve_appeal(milestone_id)','close_program(program_id)'].map(x=><code key={x}>{x}</code>)}</div></Doc><Doc title="API Reference" icon={<Code2/>}><p>Views: <code>get_program(program_id)</code>, <code>get_milestone(milestone_id)</code>, and <code>get_counts()</code>. All return JSON strings decoded by the frontend.</p></Doc><Doc title="FAQ" icon={<CircleHelp/>}><h3>Why can resolution take minutes?</h3><p>Resolution and appeal writes invoke GenLayer consensus and independent nondeterministic adjudication, so they are fundamentally slower than a normal local UI update.</p><h3>Can I change criteria after submission?</h3><p>No. The contract only allows milestone definition while the program is <code>DRAFT</code>; locking precommits the scope.</p><h3>What does INCONCLUSIVE mean?</h3><p>It is a real reachable outcome for materially insufficient/ambiguous canonical evidence or identifier-binding failure.</p></Doc></div>}
function Doc({title,icon,children}){return <section className="doc-card"><div className="doc-title">{icon}<h2>{title}</h2></div>{children}</section>}
