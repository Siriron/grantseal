import React from 'react'; import { createRoot } from 'react-dom/client'; import { BrowserRouter,Routes,Route } from 'react-router-dom'; import './styles.css'; import ErrorBoundary from './components/ErrorBoundary'; import Layout from './components/Layout'; import Dashboard from './pages/Dashboard'; import Programs from './pages/Programs'; import NewProgram from './pages/NewProgram'; import ProgramDetail from './pages/ProgramDetail'; import Docs from './pages/Docs'; import NotFound from './pages/NotFound'; import { useWallet } from './hooks/useWallet'; import { useGrantSeal } from './hooks/useGrantSeal'
function App(){const wallet=useWallet();const data=useGrantSeal(wallet.account,wallet.ensureChain);return <Layout wallet={wallet}><Routes><Route path="/" element={<Dashboard data={data} wallet={wallet}/>}/><Route path="/programs" element={<Programs data={data}/>}/><Route path="/programs/:id" element={<ProgramDetail data={data} wallet={wallet}/>}/><Route path="/new" element={<NewProgram data={data} wallet={wallet}/>}/><Route path="/docs" element={<Docs/>}/><Route path="*" element={<NotFound/>}/></Routes></Layout>}
createRoot(document.getElementById('root')).render(<React.StrictMode><ErrorBoundary><BrowserRouter><App/></BrowserRouter></ErrorBoundary></React.StrictMode>)
