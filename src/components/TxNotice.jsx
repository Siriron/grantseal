import { ExternalLink, CheckCircle2, Clock3 } from 'lucide-react'; export default function TxNotice({tx,explorerTx}){if(!tx)return null;const timeout=tx.status==='TIMEOUT';return <div className={`tx-notice ${timeout?'amber':''}`}><div>{timeout?<Clock3/>:<CheckCircle2/>}<div><b>{timeout?'Consensus is still processing':'Transaction accepted'}</b><span>{tx.functionName} · {timeout?'Do not blindly resubmit the same write.':'State was refreshed after acceptance.'}</span></div></div><a href={explorerTx(tx.hash)} target="_blank" rel="noreferrer">View transaction <ExternalLink size={14}/></a></div>}
