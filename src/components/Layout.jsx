import { Link, NavLink, useLocation } from 'react-router-dom'; import { ShieldCheck, LayoutDashboard, FolderKanban, PlusCircle, BookOpen, ExternalLink, Menu, X } from 'lucide-react'; import { useState } from 'react'; import WalletButton from './WalletButton'; import { explorerAddress } from '../config/chains'
const nav=[['/',LayoutDashboard,'Dashboard'],['/programs',FolderKanban,'Programs'],['/new',PlusCircle,'Create'],['/docs',BookOpen,'Docs']]
export default function Layout({children,wallet}){const [open,setOpen]=useState(false);const loc=useLocation();return <div className="app-shell"><header><Link className="brand" to="/"><span className="brand-mark"><ShieldCheck size={22}/></span><span>Grant<span>Seal</span></span></Link><nav className={open?'open':''}>{nav.map(([to,I,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><I size={17}/>{label}</NavLink>)}</nav><div className="header-actions"><a className="icon-link" href={explorerAddress()} target="_blank" rel="noreferrer" title="Contract explorer"><ExternalLink size={18}/></a><WalletButton wallet={wallet}/><button className="menu-btn" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header><main key={loc.pathname}>{children}</main><footer><span>GrantSeal · public milestone accountability on GenLayer StudioNet</span><a href={explorerAddress()} target="_blank" rel="noreferrer">Contract 0x7397…52ab <ExternalLink size={13}/></a></footer></div>}
